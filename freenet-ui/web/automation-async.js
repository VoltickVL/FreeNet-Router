(() => {
  'use strict';
  // Legacy Settings v2 async UI retired by Settings v3. Keep this asset as a
  // no-op compatibility boundary for older cached accepted-ux loaders.
  window.__freenetLegacyAutomationAsyncRetired = true;
})();
